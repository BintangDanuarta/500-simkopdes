# Ignore the generated main page if this repo is used as a template
/index.html

# macOS system files
.DS_Store

# Node.js artifacts (if any frontend tooling is added later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# VS Code workspace settings
.vscode/
